export default function useSaveTimer() {
  // const shouldSave = useSelector((state: RootState) => state.timerReducer.shouldSave);
  // if (shouldSave)
  // const timer: BigTimerForm = {
  //   createdAt: Date.now(),
  //   timeSeconds: useSelector(
  //     (state: RootState) => state..settings.bigTimeSeconds
  //   ),
  //   goalTimers: useSelector((state: RootState) => state..settings.goalTimers),
  // };
  // const timer: TimerForm = {
  //   createdAt: Date.now(),
  //   timeSeconds: useSelector((state: RootState) => state..settings.timeSeconds),
  //   bigTimeId: useSelector((state: RootState) => state..bigTimer?.id),
  //   taskId: useSelector((state: RootState) => state..selectedTask?.id),
  // };
  throw new Error("Function not implemented.");
}
